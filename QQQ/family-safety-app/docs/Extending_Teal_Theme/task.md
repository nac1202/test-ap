# Tealテーマの拡張

- [ ] **安否確認ページ (`/safety`)**
  - [ ] **ヘッダー**: テキスト色を `slate-800` に更新
  - [ ] **StatusForm**:
    - [ ] コンテナを `rounded-xl`, `border-slate-100`, `shadow-sm` に更新
    - [ ] "無事"ボタンを Teal/Green テーマに更新
    - [ ] "SOS"ボタンを更新（赤色は維持しつつスタイル調整）
    - [ ] "不明"ボタンを更新（黄色は維持しつつスタイル調整）
    - [ ] 入力フォーカス時のリングを `teal-500` に更新
    - [ ] 送信ボタンを `bg-teal-600` に更新

- [ ] **マップページ (`/map`)**
  - [ ] **ヘッダー**: 可能であれば `bg-white/80 backdrop-blur` に、テキストは `slate-800` に更新
  - [ ] **LocationMap**:
    - [ ] コンテナのスタイル更新（カードデザイン適用）
    - [ ] "APIキーなし"などのエラーステートのスタイル調整

- [ ] **その他のガイドページ**
  - [ ] `GuideList` コンポーネントの更新が反映されているか確認
  - [ ] `src/app/firstaid/page.tsx` と `src/app/security/page.tsx` のヘッダーを確認

- [ ] **検証**
  - [ ] 全ページでヘッダーサイズとテキスト色が一貫しているか確認
  - [ ] 全てのインタラクティブ要素（ボタン等）のホバー/フォーカス状態を確認
