# Phase 5: 資産推移の半永久記録と時間足変更システム タスクリスト

## バックエンド API・通信層
- [x] `api_server.py`: 古い履歴（HH:MM）読み込み時に自動で日付（YYYY-MM-DD）を追加補完するマイグレーションロジックの実装
- [x] `api_server.py`: 容量制限（過去96件で削除）を撤廃し、データを半永久的に `equity_history.json` に追記保存するよう改修
- [x] `api_server.py`: 軽量化のため、5秒間隔の `/api/status` 通信から `history` の巨大データをパージ（削除）する
- [x] `api_server.py`: それに代わる、履歴専用を取り出すための高速な `/api/history` エンドポイントを新設

## フロントエンド UI層
- [x] `dashboard/index.html`: グラフエリアの上部に時間足を切り替えるためのトグルボタン（15分 / 1時間 / 4時間 / 1日）を設置
- [x] `dashboard/app.js`: 新設した `/api/history` と独立して通信を行い、履歴データだけを定期または切り替え時に取得する関数を追加
- [x] `dashboard/app.js`: ボタンで選択された時間足（1h, 4h, 1dなど）に合わせて、JS内で巨大な点群データを「最後の時間」のものに圧縮・リサンプリングする描画関数を実装
