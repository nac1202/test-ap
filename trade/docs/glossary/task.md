# 用語解説機能（ビギナーサポート）実装タスク

- [x] `dashboard/index.html` の末尾に解説ポップアップ（モーダル）用のHTML構造を追加する
- [x] `dashboard/index.html` の各専門用語の横に `❓` アイコン（クリックイベント付き）を追加する
- [x] `dashboard/style.css` に `❓` アイコンと解説モーダル（グラスモーフィズム）のCSSを追加する
- [x] `dashboard/app.js` に各用語の解説文データを定義し、モーダルを開閉するJavaScriptロジックを追加する
