# タスクリスト：段階的な上位称号システムの実装

- [x] 1. `public/js/collection.js` に `getMasterRank()` 関数を追加し、全24種類のカードの「最低取得回数」から称号ランク（0, 1, 5, 10, 20）を判定するロジックを実装する。
- [x] 2. `openCollectionModal()` において、`getMasterRank()` の結果に基づきバッジ画像（`<img>`）のパスを切り替える処理を追加する。
- [x] 3. 称号ランクに応じて、コレクション画面の説明テキスト（「真理の到達者〜」）を変化させる処理を実装する。
- [x] 4. 称号ランクに応じて、背景のオーブの量・速度、モーダルの枠線色（プラチナ、赤金、虹色など）を豪華にする処理を追加する。
