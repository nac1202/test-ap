# タスクリスト: 紹介キャスト情報記録・連携機能の実装

- `[x]` **バックエンド(API)の改修**
  - `[x]` `api/index.js` の `/api/reserve` エンドポイントを修正し、リクエストに `introCast` パラメータを追加する。
  - `[x]` 保存時の `newReservation` オブジェクトに `introCast` を含める。
- `[x]` **フロントエンド(お客様用ページ)の改修**
  - `[x]` `public/index.html` の HTML 構造に「紹介キャスト選択モーダル」のテンプレートを追加する。
  - `[x]` モーダル内のセレクトボックスを `castMaster` (現在のキャスト一覧) と連動するように動的に生成する（「なし」の選択肢は設けない）。
  - `[x]` アプリデータ取得後 (`updateUI`)、`localStorage` に紹介キャストの記録がない場合のみモーダルを表示する処理を追加する。
  - `[x]` 予約送信時 (`submitReservation`) に `localStorage` から記録済みの紹介キャスト情報を読み込んで API へ一緒に送信する。
- `[x]` **フロントエンド(管理者用ページ)の改修**
  - `[x]` `public/admin.html` の モーダル内予約一覧表示ロジック (`openModal`) を修正し、予約客の名前の横に `[紹介: キャスト名]` の形で目立つように表示する。

※URLやインフラ構成には一切変更を加えない。
